import Styled from 'styled-components';

const Style = Styled.article`
    display: flex;
    justify-content: center;
    padding: 30px 0px;
    .chartWrapper {
        width: 90%;
    }
`;
export { Style };
